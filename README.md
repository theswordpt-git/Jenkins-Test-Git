# Jenkins-Test-Git
Making sure Jenkins is setup properly.

If you have Terraform, Git, AWSCLI, and Jenkins set up, when you add this repo as a job, it should create an empty s3 bucket.
Run the Destroy All Jenkinsfile to remove everything.
